{% extends 'layout_blog.html.twig' %}

{% block title %}
	Modifier une catégorie
{% endblock %}

{% block body %}

	<a href="{{ path('app_category_index') }}" class="btn btn-outline-primary">
		&nbsp;Retour à la liste des catégories
	</a>
	<hr>

	<div class="d-flex justify-content-between">
		<a href="{{ path('app_category_show', {'id': category.id }) }}" class="btn btn-outline-info">Détail de la catégorie</a>
		{{ include('category/_delete_form.html.twig') }}
	</div>

	<h1 class="text-center">Modifier une catégorie</h1>

	{{ include('category/_form.html.twig', {'button_label': 'Modifier'}) }}


{% endblock %}
