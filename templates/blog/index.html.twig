{% extends 'layout_blog.html.twig' %}

{% block title %}
	Mes articles
{% endblock %}

{% block body %}
	<h1 class="text-center">Liste des articles du Blog</h1>

	<a href="{{ path('app_blog_new') }}" class="btn btn-outline-primary">Create new</a>

	<table class="table table-hover text-center">
		<thead>
			<tr>
				<th>id</th>
				<th>Title</th>
				<th>Author</th>
				<th>Content</th>
				<th>Image</th>
				<th>CreatedAt</th>
				<th>Actions</th>
			</tr>
		</thead>
		<tbody>
			{% for article in articles %}
				<tr>
					<td>{{ article.id }}</td>
					<td>
						<a href="{{ path('app_blog_show', {'id': article.id }) }}">{{ article.title }}</a>
					</td>
					<td>{{article.user.fullName}}</td>
					<td>{{ article.content }}</td>
					<td>
						<img src={{ article.imageUrl }} width="100" height="100">
					</td>
					<td>{{ article.createdAt|date('d/m/Y') }}</td>
					<td>
						<a href="{{ path('app_blog_show', {'id': article.id }) }}" class="btn btn-outline-secondary">show</a>
						<a href="{{ path('app_blog_indexOld', {'id': article.id, 'nom': 'blablabla' }) }}" class="btn btn-outline-info">edit</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="7" class="text-center">Aucun article pour le moment.</td>
				</tr>
			{% endfor %}
		</tbody>
	</tbody>
</table>{% endblock %}
