<nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light">
	<a class="navbar-item me-auto ms-2" href="{{ path('jo_app_home') }}">
		<img src="{{ asset('assets/images/jo_logo.png') }}" class="rounded-circle" height="100px"/>
	</a>
	<div class="container-fluid">
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse text-center" id="navbarColor02">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link {% if app.request.attributes.get('_controller') starts with 'App\\Controller\\JOHomeController' %}active{% endif %}" href="{{ path('jo_app_home') }}">
						<i class="fa-solid fa-home fa-xl"></i>
						Accueil
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link {% if app.request.attributes.get('_controller') starts with 'App\\Controller\\AthleteController' %}active{% endif %}" href="{{path('app_athlete_index')}}">
						<i class="fa-solid fa-person-running fa-xl"></i>
						Athletes
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link">
						<i class="fa-solid fa-list fa-xl"></i>
						Disciplines
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link">
						<i class="fa-solid fa-globe fa-xl"></i>
						Pays
					</a>
				</li>
			</ul>
			<ul class="navbar-nav">
				<div>
					<div class="d-flex mt-3">
						<a {# href="{{ path('login_dev', {role: 'ROLE_ADMIN'}) }}" #} class="btn btn-outline-danger me-2 {% if is_granted('ROLE_ADMIN') %}disabled{% endif %}">
							User
						</a>
						<a {# href="{{ path('login_dev', {role: 'ROLE_ENSEIGNANT'}) }}" #} class="btn btn-outline-success me-2 {% if is_granted('ROLE_ENSEIGNANT') and not is_granted('ROLE_ADMIN') %}disabled{% endif %}">
							Admin
						</a>
					</div>
					<div class="mt-2 text-body-secondary">
						(DEV) Switch de compte
					</div>
				</div>
				{% if is_granted('IS_AUTHENTICATED_FULLY') %}
					<small class="text-body-secondary text-capitalize">Bonjour,
						{# {{ app.user.Name }}  #}
					</small>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
