{% extends 'layout_jo.html.twig' %}

{% block title %}
	Modifier un athlète
{% endblock %}

{% block body %}
	<a href="{{ path('app_athlete_index') }}" class="btn btn-outline-primary">
		<i class="fa-solid fa-arrow-left fa-2xl"></i>&nbsp;Retour à la liste
	</a>

	<hr>

	<div class="d-flex justify-content-between mt-2">
		<a href="{{ path('app_athlete_show', {'id': athlete.id }) }}" class="btn btn-outline-dark">
			<i class="fa-solid fa-search fa-lg"></i>&nbsp;Détail
		</a>
		{{ include('athlete/_delete_form.html.twig') }}
	</div>

	<h1 class="text-center">Modifier un athlète</h1>

	{{ include('athlete/_form.html.twig', {'button_label': 'Modifier'}) }}

{% endblock %}
