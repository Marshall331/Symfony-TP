{% extends 'layout_jo.html.twig' %}

{% block title %}
	Modifier un athlète
{% endblock %}

{% block body %}
	<a href="{{ path('app_athlete_index') }}" class="btn btn-outline-primary">
		&nbsp;Retour à la liste
	</a>

	{{ include('athlete/_delete_form.html.twig') }}

	<div class="d-flex justify-content-between">
		<a href="{{ path('app_athlete_show', {'id': athlete.id }) }}" class="btn btn-outline-info">Détail de l'article</a>
		{{ include('article/_delete_form.html.twig') }}
	</div>

	<h1 class="text-center">Modifier un athlète</h1>
	<hr>

	{{ include('athlete/_form.html.twig', {'button_label': 'Modifier'}) }}

{% endblock %}
